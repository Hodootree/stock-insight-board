# Stock Insight Board

Node.js web app for Korean and global stock research.

## Run Locally

```bash
npm start
```

The server uses `PORT` when provided by a cloud host, otherwise it listens on `4175`.

## Render

- Root directory: `outputs/stock-insight`
- Build command: leave empty
- Start command: `node server.mjs`
